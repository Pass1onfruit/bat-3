#include<stdio.h>
 
int main ()
{
	int a, b;
	scanf ("%d %d", &a, &b);
    printf("before：a=%d\nb=%d\n\n", a, b);
    a = a ^ b;
    b = a ^ b;
    a = a ^ b;
    printf("after：a=%d\nb=%d\n", a, b);

	return 0;
}
