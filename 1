#include<stdio.h>
 
int main ()
{
	int i, j, a;
	while(scanf("%d", &a) !=EOF)
	{
		for (i = 1; i <= a; i++)
		{
			for (j = 1; j <= i; j++)
			{
				printf ("%d * %d = %d  ", i, j, i*j);
				if (i == j)
					printf ("\n");
			}
		}
	}
	return 0;
}
